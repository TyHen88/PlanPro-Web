# =============================================================================
# SMARTER PMS - Environment Variables Configuration
# =============================================================================
# Copy this file to .env.local and fill in your actual values
# =============================================================================

# =============================================================================
# API Configuration
# =============================================================================
# Base URL for your backend API
NEXT_PUBLIC_API_URL=http://localhost:9090/api

# Base URL for your frontend application
NEXT_PUBLIC_APP_URL=http://localhost:3000

# =============================================================================
# Authentication (NextAuth.js)
# =============================================================================
# Secret key for NextAuth.js - generate a random string for production
NEXTAUTH_SECRET=your-nextauth-secret-key-here

# Base path for authentication routes (optional)
NEXT_PUBLIC_AUTH_BASE_PATH=/api/auth

# =============================================================================
# Google OAuth2 Configuration
# =============================================================================
# Google OAuth Client ID (Get from Google Cloud Console)
GOOGLE_CLIENT_ID=your-google-client-id-here

# Google OAuth Client Secret (Get from Google Cloud Console)
GOOGLE_CLIENT_SECRET=your-google-client-secret-here

# OAuth Redirect URI (Should match the one configured in Google Cloud Console)
# For local development: http://localhost:3000/oauth2/redirect
# For production: https://your-domain.com/oauth2/redirect
NEXT_PUBLIC_OAUTH_REDIRECT_URI=http://localhost:3000/oauth2/redirect

# =============================================================================
# Google AI Integration
# =============================================================================
# Google Generative AI API key for Gemini integration
GOOGLE_API_KEY=your-google-ai-api-key-here

# =============================================================================
# WebSocket Configuration
# =============================================================================
# Primary WebSocket endpoint (STOMP over SockJS)
NEXT_PUBLIC_WS_URL=http://localhost:9090/ws

# Fallback WebSocket endpoint (direct WebSocket)
NEXT_PUBLIC_WS_FALLBACK_URL=ws://localhost:9090/ws

# SockJS endpoint (must be HTTP/HTTPS, not WebSocket)
NEXT_PUBLIC_SOCKJS_URL=http://localhost:9090/ws

# =============================================================================
# Security & Encryption
# =============================================================================
# Encryption key for password hashing (generate a strong random key)
NEXT_PUBLIC_PWD_ENCRYPTION_KEY=your-password-encryption-key-here

# =============================================================================
# Environment & Deployment
# =============================================================================
# Node environment (development, production, test)
NODE_ENV=development

# Next.js telemetry (disable for privacy)
NEXT_TELEMETRY_DISABLED=1

# =============================================================================
# Production URLs (update these for deployment)
# =============================================================================
# For production deployment, update these URLs:
# NEXT_PUBLIC_API_URL=https://your-api-domain.com/api
# NEXT_PUBLIC_APP_URL=https://your-app-domain.vercel.app
# NEXT_PUBLIC_WS_URL=https://your-api-domain.com/ws
# NEXT_PUBLIC_WS_FALLBACK_URL=wss://your-api-domain.com/ws
# NEXT_PUBLIC_SOCKJS_URL=https://your-api-domain.com/ws

# =============================================================================
# Optional: Database Configuration (if you add database later)
# =============================================================================
# DATABASE_URL=your-database-connection-string
# DATABASE_HOST=localhost
# DATABASE_PORT=5432
# DATABASE_NAME=your-database-name
# DATABASE_USER=your-database-user
# DATABASE_PASSWORD=your-database-password

# =============================================================================
# Optional: Email Configuration (if you add email features)
# =============================================================================
# EMAIL_SERVER_HOST=smtp.gmail.com
# EMAIL_SERVER_PORT=587
# EMAIL_SERVER_USER=your-email@gmail.com
# EMAIL_SERVER_PASSWORD=your-email-password
# EMAIL_FROM=noreply@yourdomain.com

# =============================================================================
# Optional: File Upload Configuration (if you add file upload)
# =============================================================================
# CLOUDINARY_CLOUD_NAME=your-cloudinary-cloud-name
# CLOUDINARY_API_KEY=your-cloudinary-api-key
# CLOUDINARY_API_SECRET=your-cloudinary-api-secret
# AWS_ACCESS_KEY_ID=your-aws-access-key
# AWS_SECRET_ACCESS_KEY=your-aws-secret-key
# AWS_REGION=us-east-1
# AWS_S3_BUCKET=your-s3-bucket-name
